---
title: Receta · SaaS con suscripción
description: Stripe + paywall + plan management.
order: 3
---

<StackBadge tech={["next", "supabase"]} />

**Necesitas antes:**

- Haber llegado a la **Semana 2** del curso (login + base de datos funcionando).
- Tu cuenta de Stripe con producto, precio y keys listos — el click-por-click está en [Stripe paso a paso](/docs/configuracion/stripe).

<Callout type="info">
Receta opcional, fuera del temario base. El plugin Stripe (`web/plugins/stripe/`) ya trae checkout, webhook y paywall — solo configura tu cuenta y las variables de entorno.
</Callout>

## Qué construyes

Un SaaS donde los usuarios pagan suscripción mensual para acceder a tu producto. Stripe maneja el cobro; tu app desbloquea features según el plan de cada usuario.

## Pasos

1. Sigue [Stripe paso a paso](/docs/configuracion/stripe): crear la cuenta, el producto con su precio, y copiar tus keys a `web/.env.local`. Ahí mismo activas `features.payments: true` y pegas el `stripePriceId` en tu plan de `web/config.js`.
2. Aplica la migración `013_saas_organizations.sql` en Supabase si aún no lo hiciste.
3. Cuando tengas deploy, registra el webhook en Stripe (**Paso 7** de la guía).
4. Prueba el flujo completo con la tarjeta de prueba `4242 4242 4242 4242`: pagar → volver a tu app → ver las features de plan Pro desbloqueadas.
