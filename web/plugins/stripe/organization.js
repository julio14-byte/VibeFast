/** Re-export — Fase 2: contexto de organización compartido. */
export {
  getMembershipForUser,
  getOrganizationForUser,
  requireOrgContext,
  requireOrganizationId,
  canManageTeam,
  roleLabel,
} from "@/lib/organization/context"
